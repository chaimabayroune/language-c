#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
float F,C;
printf("entrer la temperature F:");
scanf("%f",&F);
C=(F-32)/1.8;
printf("%f",C);
	return 0;
}
